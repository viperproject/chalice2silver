// https://bitbucket.org/mschwerhoff/syxc-npm/issue/5/mutual-recursive-functions-through

class Test {
  function contains(x: int): bool
    requires true
    ensures result <==> get(x) > 0 /* @Fails */
  { true }
  
  function get(x: int): int
    requires contains(x)
    ensures result != 0
  { 1 }
}