class A
{
  tracked ghost var p: A
}

class B
{
  method m(x:A)
	requires (x.p).~(A.p) == ~(A.p)
	requires {x..3} != {1, x}
	requires {x} == {x}
	requires {1} <= {..}
	requires {1} > {x}
	requires {x} < {1}
    ensures {x} >= empty<A>
	ensures {1, 2, 3} != {1..3}
	ensures empty<A> != empty<C>
	ensures (x.p).~(A.p) + ~(A.p) == (x.p).~(A.p)
	ensures {1..} * {..2} == {1..2}
	ensures {1..} - {2..} != {1}
	ensures 1 + {x} == 2
	ensures {x} + 1 == 2
	ensures !({{}} + {{1}} <= {{1,2}})
  {
    assert {} == empty<int>
	assert {..1} != {1..} && {..} != {}
	assert {2} * {x} == {}
	assert {2} + {x} != {x}
	assert {x} == {2} - {x}
	assert {x} >= empty<A> && {1, 2, 3}+empty<int> <= {..}  <==> true
	assert 1 in {0..} && {} in {{1}, {2}, empty<int>}
	assert 1 in 1 && {1} in {1}
	assert 1 in {1} && true in {true}
	assert {{{{..}}}} in empty<int>
	assert {..} in empty<int>
	assert {..} in {empty<int>}
	assert 1 !in {0..} && {} !in {{1}, {2}, empty<int>}
	assert 1 !in 1 && {1} !in {1}
	assert 1 !in {1} && true !in {true}
	assert {{{{..}}}} !in empty<int>
	assert {..} !in empty<int>
	assert {..} !in {empty<int>}
	
	assert |[1..3]| == |{1..3}|
	assert |1| == |x|
	assert |{{1,2}}| + |nil<A>| == 1
  }
}

