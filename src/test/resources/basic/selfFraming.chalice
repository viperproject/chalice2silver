class A
{
  tracked ghost var p: A
  var q: A
  var r: set<A>
  var s: int
  
  method m()
    //:: UnexpectedOutput(chalice2sil.error, /silicon/issue/42/)
    requires q.~(A.p) == r && s==34
  {}
}