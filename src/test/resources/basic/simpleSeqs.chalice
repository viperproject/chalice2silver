class A {}

class B
{
  method m(x:A)
	requires [x] == [x]
	requires [[1]][1] == [1]
    ensures {x} >= empty<A>
	ensures nil<A> != [x]
	ensures [1..] == [1] ++ [2..]
	ensures [..2] == [1..2]
  {
	assert |[1..3]| == |{1,2,3}|
  }
}

