class Test {
	var b: bool

	predicate P { true }
	
	method test()
		ensures b /* @Error 330 */
	{
		unfold P
			/* Chalice will report an error here (unfold might fail) but Syxc won't,
			 * because Syxc stops verifying this method after detecting that the
			 * postcondition is not self-framing.
			 */
	}
}