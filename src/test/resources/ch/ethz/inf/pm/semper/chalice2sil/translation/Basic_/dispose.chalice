class Main {
  var x : int;
  var y : int;
    
  method disposex(z : int)
    requires rd(x)
  {}

  method test03()
    requires acc(x) && rd(y)
    ensures rd(x) && rd(y)
  {
    call disposex(1);
    assert rd(x);
    call disposex(2);
    assert rd(x);
  }
}