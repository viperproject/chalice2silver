class Main {
    var f : int;
    var marker : int;
    method main()
        requires rd(f) && acc(marker)
        ensures rd*(f)
    {
        call sub(1);
        marker := 5;
        call sub(2);
    }

    method sub(i : int)
        requires rd(f)
        ensures rd*(f)
    {

    }
}