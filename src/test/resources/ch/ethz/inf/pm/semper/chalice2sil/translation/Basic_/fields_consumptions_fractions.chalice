class Test {
	var x: int
	
	method reqX100()
		requires acc(x, 100)
		ensures acc(x, 100)
	{}
	
	method greedyXRd()
		requires rd(x)
	{}
	
	method fail4()
		requires acc(x)
		ensures acc(x)
	{
		call reqX100()
		call greedyXRd()
		assert(acc(x, 100)) /* @Error 410 */
	}
}