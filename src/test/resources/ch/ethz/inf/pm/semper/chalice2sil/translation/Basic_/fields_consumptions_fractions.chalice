class Test {
	var x: int
	
	method reqX100()
		requires acc(x, 100)
		ensures acc(x, 100)
	{}
	
	method greedyXRd()
		requires rd(x)
	{}
	
	method fail4()
		requires acc(x)
		requires rd(x)
		ensures acc(x)
	{
		call greedyXRd()
		call reqX100()
		call greedyXRd()
		assert(acc(x, 100)) /* @Error 430 */
	}
}