class Lala {
    var next: Lala;
    var x: int;
    predicate inv {
        acc(next) && acc(x) &&
		//:: ExpectedOutput(internal:feature.unsupported)
        (next != null ==> (next.inv && unfolding next.inv in this.x > next.x))  // silicon does not support this
    }
}
