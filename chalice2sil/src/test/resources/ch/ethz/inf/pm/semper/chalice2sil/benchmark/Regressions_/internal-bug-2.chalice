class Lala {
    var x;

    predicate inv { acc(x) }

    method koko()
        requires inv
    {
        x := x + 1;  // @Error 100 // Insufficient permission to access this.x
        assert (unfolding inv in x) == old(unfolding inv in x)
    }
}

