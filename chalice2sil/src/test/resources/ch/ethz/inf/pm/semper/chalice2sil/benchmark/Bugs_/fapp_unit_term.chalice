/* The unit term was not Z3-encoded, thus any heap-independent function 
 * application failed. This bug was detectable only after bug 
 * "fapp_snapshots.chalice" had been discovered.
 */
class Bug {
	function f(a: int): int { a * a}
	
	method m(a: int) returns (r: int)
		ensures r == a * a
	{ r := f(a) }
}