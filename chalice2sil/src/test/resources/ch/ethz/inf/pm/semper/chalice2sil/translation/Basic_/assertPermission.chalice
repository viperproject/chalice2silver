class Main {
    var f : int;
    var g : int;
    method main()
        requires acc(f)
        requires rd(g)
        ensures acc(f)
        ensures rd(g)
    {
        assert acc(f);
        assert rd(g);
        assert rd(f);
    }
}