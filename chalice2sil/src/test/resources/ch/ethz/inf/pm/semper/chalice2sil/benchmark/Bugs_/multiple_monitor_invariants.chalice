/* There was a bug in ast.Class such that multiple monitor invariants were not
 * conjuncted but rather treated independently. This way, given two clauses
 *  invariant acc(x)
 *  invariant x == 0
 * the well-formedness check if the second one would fail due to insufficient 
 * permissions.
 */
class Test {
	var x: int
	var y: int
	invariant acc(x)
	invariant x != 0
	invariant acc(y)
	invariant y == 0
}