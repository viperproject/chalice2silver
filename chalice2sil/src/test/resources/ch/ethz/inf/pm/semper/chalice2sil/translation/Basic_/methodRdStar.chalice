class Main {
    var f : int;
    method main()
        requires rd(f)
        ensures rd*(f)
    {
        call sub(1);
        call sub(2);
    }

    method sub(i : int)
        requires rd(f)
        ensures rd*(f)
    {

    }
}