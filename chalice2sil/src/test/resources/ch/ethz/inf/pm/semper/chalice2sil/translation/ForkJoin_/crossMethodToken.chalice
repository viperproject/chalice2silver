class Main{
    var f : int;
    method work(x : int) returns (y : int)
        requires rd(f)
        ensures rd(f)
    {
        y := x + f;
    }

    method main()
        requires acc(f)
        ensures rd(f)
    {
        f := 3;
        fork tk := work(5);
        call client(tk);
    }

    method client(tk : token<Main.work>)
        requires acc(tk.joinable) && tk.joinable
    {
        var y : int;
        join y := tk;
    }
}